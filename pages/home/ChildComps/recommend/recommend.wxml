<!--pages/home/ChildComps/recommend.wxml-->
<view class="recommend">
  <block wx:for='{{recommend}}' wx:key="{{index}}">
    <view class="recommend-item">
      <image src="{{item.image}}" bind:load="handleImageLoad"></image>
      <view>{{item.title}}</view>
    </view>
  </block>
</view>
